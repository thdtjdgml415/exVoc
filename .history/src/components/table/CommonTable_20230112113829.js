import React from 'react'

const CommonTable = (porps) => {
  return (
    <table>
        <thead>
            <tr>
                {
                    headersName.map((item, index))
                }
            </tr>
        </thead>
    </table>
  )
}

export default CommonTable